{% extends 'base.html.twig' %}

{% form_theme profileForm 'bootstrap_5_layout.html.twig' %}

{% block title %} Profil de l'utilisateur
{% endblock %}



{% block body %}


    <div class="row col-md-10">
	{{ form_start(profileForm, { 'attr': {'class': 'row', 'novalidate': 'novalidate', } }) }}

    {{ form_row(profileForm.first_name, {'row_attr': {'class': 'col-md-6'}}) }}
    {{ form_row(profileForm.name, {'row_attr': {'class': 'col-md-6'}}) }}
    {{ form_row(profileForm.adresse, {'row_attr': {'class': 'col-md-6'}}) }}
    {{ form_row(profileForm.code_postal, {'row_attr': {'class': 'col-md-6'}}) }}
    {{ form_row(profileForm.phone_number, {'row_attr': {'class': 'col-md-6'}}) }}
    {{ form_row(profileForm._token) }}

    <button type="submit" class="btn btn-succes mt-3"> Modifier profil </button>


	{{ form_end(profileForm, {render_rest: false}) }}
    {# render_rest false fait en sorte que les champs non écrits dans cette page ne s'afficheront pas dans ma page modify profile même si UserType a d'autres champs #}
    {# affiche pas le reste des autres champs dans mon formulaire #}

    </div>

{% endblock %}
