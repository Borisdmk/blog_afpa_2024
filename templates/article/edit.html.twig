{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}


{% block body %}
    <h1>Edit Article</h1>

    {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}
    {% if app.user and 'ROLE_ADMIN' in app.user.getroles()  %}
    <a class="btn btn-success mb-3" href="{{ path('app_article_index') }}">back to list</a>
    {% endif %}

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
